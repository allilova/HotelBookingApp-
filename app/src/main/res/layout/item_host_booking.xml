<?xml version="1.0" encoding="utf-8"?>
<!--
    Card layout for a single booking in the host's management dashboard.

    Shows:
    - Hotel name (which hotel this booking is for)
    - Guest name(s) and count
    - Check-in / check-out dates
    - Total price (nights × price × guests)
    - Booking timestamp
    - Status badge (color-coded)
    - Confirm button (only for PENDING bookings)
    - Cancel button (only for PENDING and CONFIRMED bookings)
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Hotel name header -->
        <TextView
            android:id="@+id/tvHostBookingHotelName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="17sp"
            android:textStyle="bold"
            android:textColor="@color/primary_blue"
            android:layout_marginBottom="8dp" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/gray_bg"
            android:layout_marginBottom="8dp" />

        <!-- Guest info row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_guest_info"
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                android:textStyle="bold"
                android:minWidth="80dp" />

            <TextView
                android:id="@+id/tvHostBookingGuestNames"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="13sp"
                android:textColor="@color/black" />
        </LinearLayout>

        <!-- Dates row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_dates"
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                android:textStyle="bold"
                android:minWidth="80dp" />

            <TextView
                android:id="@+id/tvHostBookingDates"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="13sp"
                android:textColor="@color/black" />
        </LinearLayout>

        <!-- Price row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_total"
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                android:textStyle="bold"
                android:minWidth="80dp" />

            <TextView
                android:id="@+id/tvHostBookingPrice"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/accent_orange" />
        </LinearLayout>

        <!-- Booked at row -->
        <TextView
            android:id="@+id/tvHostBookingBookedAt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="@color/text_secondary"
            android:layout_marginBottom="12dp" />

        <!-- Bottom row: status badge + action buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!--
                Status badge:
                PENDING   → gray background
                CONFIRMED → green (teal_available)
                CANCELLED → red
            -->
            <TextView
                android:id="@+id/tvHostBookingStatus"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:text="@string/status_pending"
                android:textColor="@color/white"
                android:textSize="12sp"
                android:textStyle="bold"
                android:background="@drawable/bg_status_badge"
                android:gravity="center" />

            <!--
                Confirm button: only visible when status == PENDING
                Tapping this sets status to CONFIRMED and notifies the guest
            -->
            <Button
                android:id="@+id/btnHostConfirm"
                style="@style/Widget.Material3.Button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/btn_confirm_booking"
                android:backgroundTint="@color/teal_available"
                android:layout_marginStart="8dp"
                android:visibility="gone" />

            <!--
                Cancel button: visible when status == PENDING or CONFIRMED
                Tapping this sets status to CANCELLED and notifies the guest
            -->
            <Button
                android:id="@+id/btnHostCancel"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/btn_cancel_booking"
                android:textColor="#F44336"
                android:layout_marginStart="4dp"
                android:visibility="gone" />

        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>